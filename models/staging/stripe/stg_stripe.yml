version: 2

models:
  - name: stg_payments
    description: One unique payment per row
    columns: 
      - name: payment_id
        description: Each payment has a unique ID
      - name: order_id
        description: Each order has a unique ID
      - name: payment_method
        status: Customers can use payment methods such as credit card, bank transfer, coupon, or gift card.
      - name: status
        description: The status can be a success or a fail.
      - name: created_at
        description: The date is the when the payment was recorded.